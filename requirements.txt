# =============================================================================
# AgriWorld — 工程依赖清单
# =============================================================================
# 用法:
#   最小训练环境:   pip install -r requirements.txt --no-deps  # 跳过 GEE
#   完整环境:       pip install -r requirements.txt
#   数据收集环境:   pip install -r requirements.txt && pip install earthengine-api
# =============================================================================

# ── 核心 ML (训练 & 推理必装) ─────────────────────────────────────────
torch>=2.0
torchdiffeq>=0.2.0
numpy>=1.22

# ── 数据处理 (训练必装) ──────────────────────────────────────────────
pandas>=1.5
scipy>=1.7            # trainer / loss 辅助

# ── 数据收集管线 (GEE & USDA, 仅 data_pipeline.py 需要) ──────────────
earthengine-api>=0.1.340
google-auth>=2.0
geopandas>=0.12
shapely>=2.0
requests>=2.28
urllib3>=1.26

# ── 可视化 / 日志 (可选) ──────────────────────────────────────────────
# matplotlib>=3.6      # 绘制 LAI / yield 收敛曲线
# tensorboard>=2.10     # 训练日志可视化
